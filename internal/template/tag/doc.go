// Package tag builds the richtext markup that the CLI's output templates and
// renderers emit.
//
// It exists to keep the tag syntax in one place rather than scattered across the
// renderers.
package tag
