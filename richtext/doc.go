// Package richtext renders bracketed tag markup into ANSI-styled terminal
// output, gated by the terminal's colour capability. A [Writer] performs the
// rendering as an [io.Writer].
package richtext
